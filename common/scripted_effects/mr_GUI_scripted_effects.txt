########################
# GUI SCRIPTED EFFECTS #
########################

#by Lord R

#############################

### Morgenröte Main Window Toggle Effect ###
#Uses variables to activate the Morgenröte Window and toggle through it.
mr_set_gui_variables_for_mr_windows_effect = {
	#Sets up all the rankings
	verrier_set_estimated_yearly_astronomical_objects_effect = yes
	vikelas_set_member_states_effect = yes
	vikelas_ioc_board_members_effect = yes
	artists_set_yearly_tradition_gain_variable = yes
	artists_set_up_travelling_composer_list_effect = yes
	artists_set_up_local_writer_list_effect = yes
	artists_set_up_independent_painter_list_effect = yes
}

mr_cycle_tabs = {
	if = {
		# When there are more tabs cycle to next one
		limit = {
			has_variable = $tab_variable$
			var:$tab_variable$ < $tab_count$
		}
		change_variable = {
			name = $tab_variable$
			add = 1
		}
	}
	else_if = {
		limit = {
			has_variable = $tab_variable$
			var:$tab_variable$ >= $tab_count$
		}
		set_variable = mr_close_windows
		remove_variable = $tab_variable$
	}
	else = {
		# If there is no variable we set it
		set_variable = {
			name = $tab_variable$
			value = 1
		}
	}
}

#This effect is used to set a variable to the estimated yearly astronomical objects
verrier_set_estimated_yearly_astronomical_objects_effect = {
	set_variable = {
		name = verrier_set_estimated_yearly_astronomical_objects_var
		value = verrier_yearly_astronomical_objects_value
	}
}

#This effect sets up all the variables for the Olympic Overview
vikelas_set_member_states_effect = {

	#Removes all GUI variables
	remove_variable = GUI_vikelas_athlete_var
	remove_global_variable = vikelas_ioc_president_global_var
	remove_variable = GUI_vikelas_next_host_var

	#Calculates how many Participants there are
	vikelas_calculate_number_participants_effect = yes

	#Sets the IOC President as a variable
	if = {
		limit = {
			any_country = {
				has_variable = vikelas_ioc_founding_country_var
			}
		}
		random_country = {
			limit = {
				has_variable = vikelas_ioc_founding_country_var
			}
			save_scope_as = ioc_president_scope
		}
		set_global_variable = {
			name = vikelas_ioc_president_global_var
			value = scope:ioc_president_scope
		}
	}

	#Sets a random Athlete as a variable
	if = {
		limit = {
			any_scope_character = {
				OR = {
					has_trait = vikelas_athlete
					has_trait = vikelas_winner
				}
			}
		}
		random_scope_character = {
			limit = {
				OR = {
					has_trait = vikelas_athlete
					has_trait = vikelas_winner
				}
			}
			save_scope_as = GUI_vikelas_athlete_scope
		}
		set_variable = {
			name = GUI_vikelas_athlete_var
			value = scope:GUI_vikelas_athlete_scope
		}
	}

	#Sets the next host as a variable
	if = {
		limit = {
			any_country = {
				OR = {
					has_modifier = vikelas_olympics_preparation_modifier
					has_modifier = vikelas_olympics_hosting_modifier
				}
			}
		}
		random_country = {
			limit = {
				OR = {
					has_modifier = vikelas_olympics_preparation_modifier
					has_modifier = vikelas_olympics_hosting_modifier
				}
			}
			save_scope_as = GUI_vikelas_next_host_scope
		}
		set_variable = {
			name = GUI_vikelas_next_host_var
			value = scope:GUI_vikelas_next_host_scope
		}
	}
}

#########################################################################

#Sets the yearly cultural tradition variable when the window gets opened.
artists_set_yearly_tradition_gain_variable = {
	every_country = {
		set_variable = {
			name = elgar_yearly_musical_tradition_var
			value = elgar_yearly_musical_tradition_value
		}
		set_variable = {
			name = manzoni_yearly_literary_tradition_var
			value = manzoni_yearly_literary_tradition_value
		}
		set_variable = {
			name = klimt_yearly_painting_tradition_var
			value = klimt_yearly_painting_tradition_value
		}
	}
}

### Chemical Score Ranking List ###
#Creates a list of the countries ranked by their chemical score.
#As this effect is called in an effect, it creates a global variable list.
mendelejew_create_chemical_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_chemical_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:mendelejew_chemical_score_country_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = mendelejew_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_chemical_ranking_global_list
			target = this
		}
	}
}

### Theorized Elements Ranking List ###
#Creates a list of the countries ranked by their theorized elements.
#As this effect is called in an effect, it creates a global variable list.
mendelejew_create_theorized_element_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_theorized_element_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:mendelejew_theorized_elements_country_number_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = mendelejew_theorized_element_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_theorized_element_ranking_global_list
			target = this
		}
	}
}

### Discovered Elements Ranking List ###
#Creates a list of the countries ranked by their discovered elements.
#As this effect is called in an effect, it creates a global variable list.
mendelejew_create_discovered_element_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_discovered_element_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:mendelejew_discovered_elements_country_number_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = mendelejew_discovered_element_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_discovered_element_ranking_global_list
			target = this
		}
	}
}

### Isolated Elements Ranking List ###
#Creates a list of the countries ranked by their isolated elements.
#As this effect is called in an effect, it creates a global variable list.
mendelejew_create_isolated_element_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_isolated_element_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:mendelejew_isolated_elements_country_number_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = mendelejew_isolated_element_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_isolated_element_ranking_global_list
			target = this
		}
	}
}

### Astronomical Prestige List ###
#Creates a list of the countries ranked by their astronomical prestige.
#As this effect is called in an effect, it creates a global variable list.
verrier_create_astronomical_prestige_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_astronomical_prestige_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	#Makes sure the variable is up to date
	every_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}

		set_variable = {
			name = verrier_astronomical_prestige_var
			value = verrier_astronomical_prestige
		}
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:verrier_astronomical_prestige_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = verrier_astronomical_prestige_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_astronomical_prestige_ranking_global_list
			target = this
		}
	}
}

### Planets Ranking List ###
#Creates a list of the countries ranked by their discovered planets.
#As this effect is called in an effect, it creates a global variable list.
verrier_create_planets_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_planets_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:verrier_num_discovered_planets_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = verrier_planets_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_planets_ranking_global_list
			target = this
		}
	}
}

### Moons Ranking List ###
#Creates a list of the countries ranked by their discovered moons.
#As this effect is called in an effect, it creates a global variable list.
verrier_create_moons_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_moons_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:verrier_num_discovered_moons_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = verrier_moons_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_moons_ranking_global_list
			target = this
		}
	}
}

### Astronomical Objects Ranking List ###
#Creates a list of the countries ranked by their astronomical objects.
#As this effect is called in an effect, it creates a global variable list.
verrier_create_astronomical_objects_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_astronomical_objects_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:verrier_astronomical_objects
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = verrier_astronomical_objects_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_astronomical_objects_ranking_global_list
			target = this
		}
	}
}

### Geological Score Ranking List ###
#Creates a list of the countries ranked by their geological score.
#As this effect is called in an effect, it creates a global variable list.
agassiz_create_geological_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_geological_score_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	every_country = {
		set_variable = {
			name = agassiz_country_age_of_earth_calculation_var
			value = agassiz_country_age_of_earth_calculation_value
		}
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:agassiz_country_age_of_earth_calculation_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = agassiz_geological_score_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_geological_score_ranking_global_list
			target = this
		}
	}
}

### Geological Knowledge Ranking List ###
#Creates a list of the countries ranked by their geological knowledge.
#As this effect is called in an effect, it creates a global variable list.
agassiz_create_geological_knowledge_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_geological_knowledge_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:agassiz_geological_knowledge_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = agassiz_geological_knowledge_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_geological_knowledge_ranking_global_list
			target = this
		}
	}
}

### Oceanographic Knowledge Ranking List ###
#Creates a list of the countries ranked by their oceanographic knowledge.
#As this effect is called in an effect, it creates a global variable list.
agassiz_create_oceanographic_knowledge_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_oceanographic_knowledge_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:rapanui_oceanographic_understanding
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = agassiz_oceanographic_knowledge_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_oceanographic_knowledge_ranking_global_list
			target = this
		}
	}
}

### Minerals Ranking List ###
#Creates a list of the countries ranked by the number of their minerals.
#As this effect is called in an effect, it creates a global variable list.
agassiz_create_minerals_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_minerals_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:agassiz_generic_minerals_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = agassiz_minerals_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_minerals_ranking_global_list
			target = this
		}
	}
}

### Rare Minerals Ranking List ###
#Creates a list of the countries ranked by the number of their rare minerals.
#As this effect is called in an effect, it creates a global variable list.
agassiz_create_rare_minerals_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_rare_minerals_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:agassiz_rare_minerals_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = agassiz_rare_minerals_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_rare_minerals_ranking_global_list
			target = this
		}
	}
}

### Specimen Ranking List ###
#Creates a list of the countries ranked by their specimens.
#As this effect is called in an effect, it creates a global variable list.
dubois_create_specimen_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_specimen_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:dubois_specimen
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = dubois_specimen_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_specimen_ranking_global_list
			target = this
		}
	}
}

### Zoo Animal Ranking List ###
#Creates a list of the countries ranked by their zoo animals.
#As this effect is called in an effect, it creates a global variable list.
dubois_create_zoo_animal_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_zoo_animal_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:dubois_zoo_animal_total
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = dubois_zoo_animal_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_zoo_animal_ranking_global_list
			target = this
		}
	}
}

### Animal Documentation Ranking List ###
#Creates a list of the countries ranked by their animal documentations.
#As this effect is called in an effect, it creates a global variable list.
dubois_create_animal_documentation_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_animal_documentation_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:dubois_country_num_animal_documentation_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = dubois_animal_documentation_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_animal_documentation_ranking_global_list
			target = this
		}
	}
}

### Dinosaur Skeleton Ranking List ###
#Creates a list of the countries ranked by their dinosaur skeletons.
#As this effect is called in an effect, it creates a global variable list.
dubois_create_dinosaur_skeleton_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_dinosaur_skeleton_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:dubois_dinosaur_skeleton
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = dubois_dinosaur_skeleton_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_dinosaur_skeleton_ranking_global_list
			target = this
		}
	}
}

### Fossil Ranking List ###
#Creates a list of the countries ranked by their fossils.
#As this effect is called in an effect, it creates a global variable list.
dubois_create_fossil_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_fossil_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:dubois_fossil
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = dubois_fossil_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_fossil_ranking_global_list
			target = this
		}
	}
}

### Dinosaur Bone Ranking List ###
#Creates a list of the countries ranked by their dinosaur bones.
#As this effect is called in an effect, it creates a global variable list.
dubois_create_dinosaur_bone_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_dinosaur_bone_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:dubois_dinosaur_bone_total
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = dubois_dinosaur_bone_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_dinosaur_bone_ranking_global_list
			target = this
		}
	}
}

### Pathogen Ranking List ###
#Creates a list of the countries ranked by their pathogen.
#As this effect is called in an effect, it creates a global variable list.
theiler_create_pathogen_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_pathogen_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			has_variable = theiler_num_disease_pathogens_var
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:theiler_num_disease_pathogens_var
		max = 1000
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = theiler_pathogen_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_pathogen_ranking_global_list
			target = this
		}
	}
}

### Vaccine Ranking List ###
#Creates a list of the countries ranked by their vaccine.
#As this effect is called in an effect, it creates a global variable list.
theiler_create_vaccine_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_vaccine_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			has_variable = theiler_num_disease_vaccines_var
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:theiler_num_disease_vaccines_var
		max = 1000
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = theiler_vaccine_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_vaccine_ranking_global_list
			target = this
		}
	}
}

### Microorganism Ranking List ###
#Creates a list of the countries ranked by their microorganisms.
#As this effect is called in an effect, it creates a global variable list.
theiler_create_microorganisms_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_microorganisms_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:theiler_microorganisms_var
		max = 1000
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = theiler_microorganisms_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_microorganisms_ranking_global_list
			target = this
		}
	}
}

### Pharmaceuticals Ranking List ###
#Creates a list of the countries ranked by their pharmaceuticals.
#As this effect is called in an effect, it creates a global variable list.
theiler_create_pharmaceuticals_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_pharmaceuticals_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			has_variable = theiler_num_pharmaceuticals_var
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:theiler_num_pharmaceuticals_var
		max = 1000
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = theiler_pharmaceuticals_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_pharmaceuticals_ranking_global_list
			target = this
		}
	}
}

### Biologists Ranking List ###
#Creates a list of the countries ranked by the experience of their Biologists.
#As this effect is called in an effect, it creates a global variable list.
theiler_create_biologist_ranking_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_biologist_ranking_global_list

	ordered_character = {
		limit = {
			has_role = character_role_theiler_biologist
			has_variable = theiler_biologist_experience_var
		}
		order_by = var:theiler_biologist_experience_var
		check_range_bounds = no

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_biologist_ranking_global_list
			target = this
		}
	}
}

### Total Artifacts Ranking List ###
#Creates a list of the countries ranked by their total artifacts.
#As this effect is called in an effect, it creates a global variable list.
lepsius_create_total_artifact_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_total_artifact_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:lepsius_total_artifacts
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = lepsius_total_artifacts_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_total_artifact_ranking_global_list
			target = this
		}
	}
}

### Total Major Artifacts Ranking List ###
#Creates a list of the countries ranked by their total major artifacts.
#As this effect is called in an effect, it creates a global variable list.
lepsius_create_total_major_artifact_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_total_major_artifact_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:lepsius_total_major_artifacts_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = lepsius_total_major_artifacts_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_total_major_artifact_ranking_global_list
			target = this
		}
	}
}

### Total Discoveries Ranking List ###
#Creates a list of the countries ranked by their total discoveries.
#As this effect is called in an effect, it creates a global variable list.
lepsius_create_discoveries_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_discoveries_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:lepsius_total_discoveries
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = lepsius_discoveries_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_discoveries_ranking_global_list
			target = this
		}
	}
}

### Total Expeditions Ranking List ###
#Creates a list of the countries ranked by their total expeditions.
#As this effect is called in an effect, it creates a global variable list.
lepsius_create_expeditions_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_expeditions_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:lepsius_total_expeditions
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = lepsius_expeditions_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_expeditions_ranking_global_list
			target = this
		}
	}
}

### Total Anthropological Rating Ranking List ###
#Creates a list of the countries ranked by their anthropological ratin.
#As this effect is called in an effect, it creates a global variable list.
lepsius_create_anthropological_rating_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_anthropological_rating_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:andersson_anthropological_rating
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = andersson_anthropological_rating_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_anthropological_rating_ranking_global_list
			target = this
		}
	}
}

### Total Human Fossils Ranking List ###
#Creates a list of the countries ranked by their total human fossils.
#As this effect is called in an effect, it creates a global variable list.
lepsius_create_total_human_fossils_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_total_human_fossils_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:andersson_total_human_fossil
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = andersson_total_human_fossils_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_total_human_fossils_ranking_global_list
			target = this
		}
	}
}

### Musical Tradition Ranking List ###
#Creates a list of the countries ranked by their musical tradition.
#As this effect is called in an effect, it creates a global variable list.
elgar_create_musical_tradition_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_musical_tradition_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:elgar_music_tradition
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = elgar_musical_tradition_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_musical_tradition_ranking_global_list
			target = this
		}
	}
}

### Literary Tradition Ranking List ###
#Creates a list of the countries ranked by their literary tradition.
#As this effect is called in an effect, it creates a global variable list.
manzoni_create_literary_tradition_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_literary_tradition_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:manzoni_literary_tradition_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = manzoni_literary_tradition_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_literary_tradition_ranking_global_list
			target = this
		}
	}
}

### Painting Tradition Ranking List ###
#Creates a list of the countries ranked by their painting tradition.
#As this effect is called in an effect, it creates a global variable list.
klimt_create_painting_tradition_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_painting_tradition_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:klimt_painting_tradition_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = klimt_painting_tradition_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_painting_tradition_ranking_global_list
			target = this
		}
	}
}

### Mountain Ascents Ranking List ###
#Creates a list of the countries ranked by their succesful Mountain ascents.
#As this effect is called in an effect, it creates a global variable list.
dubois_create_succesful_mountain_ascents_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_succesful_mountain_ascents_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:douglas_summits_conquered_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = dubois_succesful_mountain_ascents_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_succesful_mountain_ascents_ranking_global_list
			target = this
		}
	}
}

### Olympic Medals Ranking List ###
#Creates a list of the countries ranked by their olympic gold medals.
#As this effect is called in an effect, it creates a global variable list.
vikelas_create_olympic_medals_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_olympic_medals_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:vikelas_gold_medals_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = vikelas_olympic_medals_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_olympic_medals_ranking_global_list
			target = this
		}
	}
}

### Schneider Trophy Victories Ranking List ###
#Creates a list of the countries ranked by their schneider trophy victories.
#As this effect is called in an effect, it creates a global variable list.
curtiss_create_schneider_trophy_victories_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_schneider_trophy_victories_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:curtiss_schneider_trophy_victories_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = curtiss_schneider_trophy_victories_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_schneider_trophy_victories_ranking_global_list
			target = this
		}
	}
}

### World Cup Ranking List ###
#Creates a list of the countries ranked by their world cup victories.
#As this effect is called in an effect, it creates a global variable list.
vikelas_create_world_cup_victories_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_world_cup_victories_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:vikelas_num_football_worldcup_won_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = vikelas_world_cup_victories_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_world_cup_victories_ranking_global_list
			target = this
		}
	}
}

### Grand Prix Victories Ranking List ###
#Creates a list of the countries ranked by their grand prix victories.
#As this effect is called in an effect, it creates a global variable list.
vikelas_create_grand_prix_victories_ranking_list_effect = {
	#Removes the global variable list
	clear_global_variable_list = mr_grand_prix_victories_ranking_global_list

	set_local_variable = {
		name = mr_current_rank_local_var
		value = 0
	}

	ordered_country = {
		limit = {
			is_revolutionary = no #Hopefully keeps revolutions from appearing in the ranking
			NOR = {
				c:PRC ?= this
				is_country_type = decentralized
			}
		}
		order_by = var:vikelas_grand_prix_victories_var
		check_range_bounds = no #prevents errors

		#Keeps Track of the rank so it can be saved as a variable
		change_local_variable = {
			name = mr_current_rank_local_var
			add = 1
		}
		if = {
			limit = {
				this = {
					is_player = yes
				}
			}
			set_variable = {
				name = vikelas_grand_prix_victories_player_rank_var
				value = local_var:mr_current_rank_local_var
			}
		}

		#This part adds all the countries to the list
		add_to_global_variable_list = {
			name = mr_grand_prix_victories_ranking_global_list
			target = this
		}
	}
}

#This effect removes variables set for the GUI, when the GUI is closed
GUI_remove_unnecessary_variables_effect = {
	remove_variable = verrier_set_estimated_yearly_astronomical_objects_var
	remove_variable = GUI_vikelas_athlete_var
	remove_variable = GUI_vikelas_next_host_var
	remove_variable = GUI_vikelas_athlete_var
	remove_variable = elgar_yearly_musical_tradition_var
	remove_variable = manzoni_yearly_literary_tradition_var
	remove_variable = klimt_yearly_painting_tradition_var
}